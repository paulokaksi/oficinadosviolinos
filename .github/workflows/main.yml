on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: 34.175.178.71
        username: oficinaviolinoskaksimediapt
        password: ${{ secrets.password }}
        protocol: ftps
        port: 58746 # todo replace with your web hosts ftps port
        timeout: timeout
